<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Sign Up</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <% if (cssFile) { %>
        <link rel="stylesheet" href="/<%= css %>">
        <% } %>

            <style>
                body {
                    background-color: #f8f9fa;
                }

                .signup-wrapper {
                    padding: 2rem 1rem;
                    min-height: 90vh;
                    display: flex;
                    align-items: center;
                }

                .signup-card {
                    padding: 3rem 2rem;
                }

                .form-control {
                    padding: 1rem;
                    font-size: 1.1rem;
                }

                .form-label {
                    font-size: 1.1rem;
                    margin-bottom: 0.5rem;
                }

                button.btn {
                    padding: 0.8rem;
                    font-size: 1.2rem;
                }

                h2 {
                    font-size: 2rem;
                    margin-bottom: 2rem;
                }

                @media (min-width: 768px) {
                    .signup-wrapper {
                        padding: 4rem;
                    }

                    .signup-card {
                        padding: 4rem 3rem;
                    }
                }
            </style>
</head>

<body>
    <div class="container-fluid">
        <div class="signup-wrapper">
            <div class="bg-white rounded shadow-sm w-100 mx-auto signup-card" style="max-width: 600px;">
                <h2 class="text-center">Sign Up</h2>
                <form id="signup-form">
                    <div class="mb-4">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" id="username" name="username" class="form-control" required>
                    </div>

                    <div class="mb-4">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" id="password" name="password" class="form-control" required>
                    </div>

                    <div class="mb-4">
                        <label for="confirm-password" class="form-label">Confirm Password</label>
                        <input type="password" id="confirm-password" name="confirm-password" class="form-control"
                            required>
                    </div>

                    <button id="signup_btn" type="submit" class="btn btn-primary w-100">Register</button>
                    <p id="error-msg" class="text-danger text-center mt-3"></p>
                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>

    <% if (jsFile) { %>
        <script src="/<%= js %>"></script>
        <% } %>
</body>

</html>